@{
    ViewBag.Title = "Home Page";
}

@{
    var usuario = @Session["UserName"];
    var rol = @Session["UserRole"] as string;
}

<a href="/Auth/Logout">Cerrar Sesión</a>

<div class="container mt-5">
    <div class="card shadow p-4" style="border-radius:15px;">

        <div class="mb-4">
            <h3 class="fw-bold">Dashboard</h3>
            <p class="text-muted">Bienvenido al Centro de Capacitación de Emergencias</p>
            <p class="text-muted">
                Bienvenido @usuario
            </p>
        </div>

        @if (usuario != null && rol == "Administrador")
        {
            <div class="mb-3">
                <a href="@Url.Action("Create", "Participantes")"
                   class="btn btn-primary w-100">
                    Registrar Nuevo Participante
                </a>

                <a href="@Url.Action("ListaParticipantes", "Participantes")"
                   class="btn btn btn-info w-100">
                    Modulo Participantes
                </a>

            </div>
            <div class="mb-3">
                <a href="@Url.Action("Create", "Cohortes")"
                   class="btn btn-primary w-100">
                    Registrar Nuevo Cohorte
                </a>

            </div>
            <div class="mb-3">
                <a href="@Url.Action("Lista", "Cohortes")"
                   class="btn btn-primary w-100">
                    Lista de Cohortes
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("ListaArchivados", "Cohortes")"
                   class="btn btn-warning w-100">
                    Lista de Cohortes Archivados
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("Create", "Cursos")"
                   class="btn btn-primary w-100">
                    Registrar Nuevo Curso
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("Lista", "Cursos")"
                   class="btn btn-primary w-100">
                    Lista de Cursos
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("ListaArchivados", "Cursos")"
                   class="btn btn-warning w-100">
                    Lista de Cursos Archivados
                </a>
            </div>
        }

        @if (usuario != null && rol == "Instructor")
        {


            <div class="mb-3">
                <a href="@Url.Action("Seleccion","Panel")"
                   class="btn btn-sm btn-info">
                    Evaluar Destreza
                </a>
            </div>


            <div class="mb-3">
                <a href="@Url.Action("ListaParticipantes", "Participantes")"
                   class="btn btn-primary w-100">
                    Modulo Participantes
                </a>
            </div>


            <div class="mb-3">
                <a href="@Url.Action("Lista", "Cohortes")"
                   class="btn btn-primary w-100">
                    Lista de Cohortes
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("ListaArchivados", "Cohortes")"
                   class="btn btn-warning w-100">
                    Lista de Cohortes Archivados
                </a>
            </div>

            <div class="mb-3">
                <a href="@Url.Action("Lista", "Cursos")"
                   class="btn btn-primary w-100">
                    Lista de Cohortes
                </a>
            </div>


            <div class="mb-3">
                <a href="@Url.Action("ListaArchivados", "Cursos")"
                   class="btn btn-warning w-100">
                    Lista de Cursos Archivados
                </a>
            </div>
        }

    </div>
</div>