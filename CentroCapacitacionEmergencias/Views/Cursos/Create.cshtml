

@model CentroCapacitacionEmergencias.ViewModels.CursoViewModel

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
        <button class="btn btn-sm btn-outline-dark"
                onclick="this.closest('.alert').remove()">
            ✖
        </button>
    </div>
}

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <strong>Por favor corrija los siguientes errores:</strong>
        @Html.ValidationSummary(false)
    </div>
}

@if (Model.Id == 0)
{
    <h2>
        Crear Curso
    </h2>
}
else
{
    <h2>
        Modificar Curso
    </h2>
}

@using (Model.Id != 0 ? Html.BeginForm("EditarCurso", "Cursos", FormMethod.Post) : Html.BeginForm())
{
    <div class="card p-4 shadow-sm">

        @Html.HiddenFor(m => m.Id)

        <div class="mb-3">
            <label>Título</label>
            @Html.TextBoxFor(m => m.Titulo, new { @class = "form-control" })
        </div>

        <div class="mb-3">
            <label>Código Curso</label>
            @Html.TextBoxFor(m => m.CodigoCurso, new { @class = "form-control" })
        </div>

        <div class="mb-3">
            <label>Horas Prácticas</label>
            @Html.TextBoxFor(m => m.HorasPracticas, new { @class = "form-control" })
        </div>

        <div class="mb-3">
            <label>Máximo de Instructores</label>
            @Html.TextBoxFor(m => m.maxInstructores, new
            {
                @class = "form-control",
                   type = "number",
                   min = "1",
                   max = "100",
                   required = "required"
               })
        </div>

        @if (Model.Id == 0)
        {
            <label>Seleccione uno o varios cursos (Ctrl + click)</label>
            <div class="mb-3">
                <label>Instructores</label>
                @Html.ListBoxFor(m => m.InstructoresSeleccionados, Model.Instructores, new { @class = "form-control" })
            </div>

        }


        @if (Model.Id == 0)
        {
            <div class="mb-3">
                <label>Cohortes</label>
                @Html.ListBoxFor(m => m.CohortesSeleccionadas, Model.Cohortes, new { @class = "form-control" })
            </div>
        }


        @if (Model.Id != 0)
        {
            <button type="submit" class="btn btn-warning">
                Modificar Curso
            </button>

        }
        else
        {
            <button type="submit" class="btn btn-primary">
                Crear Curso
            </button>
        }


        <br />
        <a href="@Url.Action("Lista", "Cursos")" class="btn btn-secondary">
            Cancelar
        </a>
    </div>

}

